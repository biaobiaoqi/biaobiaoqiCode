//题意容易理解错，不是求总共能得到的最多利润，而是一买一卖能得到的最大利润。
public class Solution {
    public int maxProfit(int[] prices) {
        if (prices.length <= 1) return 0;
        int min = prices[0];
        int pro = 0;
        for (int i = 1; i < prices.length; i ++) {
            min = Math.min(min, prices[i]);
            pro = Math.max(pro, prices[i] - min);
        }
        return pro;
    }
}
